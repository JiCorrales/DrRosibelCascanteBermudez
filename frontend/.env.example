# Variables de entorno del frontend. Copiar este archivo a `.env.local` y rellenar.
#
# Si ambas variables están vacías, el frontend cae automáticamente a los datos
# mock de src/mock/admin-data.js + src/data.js (útil para correr tests/dev sin red).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
